# ============================================================
# FlyPass Bot — Зависимости Python
# ============================================================

# Telegram Bot Framework
aiogram==3.6.0              # Главный фреймворк бота
aiogram-dialog==2.1.0       # Диалоговые состояния (FSM)

# База данных (тот же драйвер что у API)
sqlalchemy==2.0.30
asyncpg==0.29.0

# Redis (кэш и rate-limiting)
redis==5.0.4
aioredis==2.0.1

# HTTP клиент (запросы к нашему API)
httpx==0.27.0

# Утилиты
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
loguru==0.7.2

# QR-коды (для реферальных ссылок в боте)
qrcode==7.4.2
pillow==10.3.0
